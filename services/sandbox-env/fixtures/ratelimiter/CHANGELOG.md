# Changelog

All notable changes to this project are documented here. Newest release first.

## [0.1.0] - 2026-09-01

- Initial token bucket with `try_acquire` and time-based refill.
- `load_settings` reads `config/settings.json`; `allowed_burst` derives the burst ceiling.
